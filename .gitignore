# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
~$*
*.tmp

# Backend API
api/node_modules
api/dist
api/*.log

# Env files (never commit secrets)
.env
.env.*
!.env.example

# Supabase local dev
supabase/.branches
supabase/.temp
supabase/logs

# Database dumps written by the db-backup service (docker-compose.yml).
# Contains real student data — must never be committed.
backups/

# Reference source the Chemistry Lab was ported from — not app code.
EducationAI-Games-master/
