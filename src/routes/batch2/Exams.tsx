import React from 'react';
import { ExamCenter } from '../../components/shared/ExamCenter';

// Real exam center — assigned exams from the backend, randomized papers,
// autosave, tab-switch proctoring, and scores once the teacher reviews.
export const Batch2Exams: React.FC = () => <ExamCenter accent="indigo" />;
