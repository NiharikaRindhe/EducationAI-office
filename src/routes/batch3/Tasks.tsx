import React from 'react';
import { TaskList } from '../../components/shared/TaskList';

// Same gap as Batch 2 — no Tasks page existed for Class 9-10 either.
export const Batch3Tasks: React.FC = () => <TaskList accent="sky" />;
